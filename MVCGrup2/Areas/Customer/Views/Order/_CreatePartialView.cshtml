@* @using MVCGrup2.Areas.Customer.Models
@using MVCGrup2.Entities.Concrete

@model OrderViewModel

<h4>OrderViewModel</h4>
<hr />
<div class="container-fluid">
   

   
        <form asp-action="Create" method="post">
            <div class="row " style="margin-left:20px;">

            @foreach (ExtraMat item in ViewBag.Extras)
            {
                <div class="col-md-6 mb-2">
                <div class="card text-center d-flex align-items-center" style="width: 18rem; margin-left:20px;">
                    <img class="card-img-top mx-auto" src="/Pictures/@item.PictureName" alt="@item.Name">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <p class="card-text">@item.Description</p>
                        <div class="form-check">
                            <input type="checkbox" id="ExtraMat_@item.Id" name="SelectedExtraMats" value="@item.Id" />
                            <label class="form-check-label" for="ExtraMat_@item.Id">Select</label>
                        </div>
                        <div class="form-group">
                            <label asp-for="OrderCounts[@item.Id]" class="control-label"></label>
                            <input type="number" id="OrderCount_@item.Id" name="OrderCounts_@item.Id" class="form-control" onchange="updateTotal(@item.Price, '@item.Id')" value="1" min="1" />
                            <span asp-validation-for="OrderCounts[@item.Id]" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label class="control-label">Total</label>
                            <span id="Total_@item.Id">@item.Price</span>
                        </div>
                    </div>
                </div>
                </div>
            }
            @foreach (Menu item in ViewBag.Menus)
            {
                <div class="col-md-6 mb-2">
                    <div class="card text-center d-flex align-items-center" style="width: 18rem;margin-left:20px">
                    <img class="card-img-top mx-auto" src="/Pictures/@item.PictureName" alt="@item.Name">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <p class="card-text">@item.Description</p>
                        <div class="form-check">
                            <input type="checkbox" id="ExtraMat_@item.Id" name="SelectedExtraMats" value="@item.Id" />
                            <label class="form-check-label" for="ExtraMat_@item.Id">Select</label>
                        </div>
                        <div class="form-group">
                            <label asp-for="OrderCounts[@item.Id]" class="control-label"></label>
                            <input type="number" id="OrderCount_@item.Id" name="OrderCounts_@item.Id" class="form-control" onchange="updateTotal(@item.Price, '@item.Id')" value="1" min="1" />
                            <span asp-validation-for="OrderCounts[@item.Id]" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label class="control-label">Total</label>
                            <span id="Total_@item.Id">@item.Price</span>
                        </div>
                    </div>
                </div> 
                </div>
            }
          
</div>
 </form>
            <button type="submit" class="btn btn-primary">Submit Order</button>
     
   
</div>
 *@
@* @using MVCGrup2.Areas.Customer.Models
@using MVCGrup2.Entities.Concrete

@model OrderViewModel

<h4>OrderViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <h5>Extras</h5>
        <div class="row">
            @foreach (ExtraMat item in ViewBag.Extras)
            {
                <div class="col-md-6 mb-2 ">
                    <div class="card text-center d-flex align-item-center" style="width: 18rem;margin:20px">
                        <img class="card-img-top img-fluid mx-auto" src="/Pictures/@item.PictureName"  style="max-width:50%" alt="@item.Name">
                        <div class="card-body">
                            <h5 class="card-title">@item.Name</h5>
                            <p class="card-text">@item.Description</p>
                            <div class="form-check">
                                <input type="checkbox" id="ExtraMat_@item.Id" name="SelectedExtraMats" value="@item.Id" />
                                <label class="form-check-label" for="ExtraMat_@item.Id">Select</label>
                            </div>
                            <div class="form-group">
                                <label asp-for="OrderCounts[@item.Id]" class="control-label"></label>
                                <input type="number" id="OrderCount_@item.Id" name="OrderCounts_@item.Id" class="form-control" onchange="updateTotal(@item.Price, '@item.Id')" value="1" min="1" />
                                <span asp-validation-for="OrderCounts[@item.Id]" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label class="control-label">Total</label>
                                <span id="Total_@item.Id">@item.Price</span>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
    <div class="col-md-6">
        <h5>Menus</h5>
        <div class="row">
            @foreach (Menu item in ViewBag.Menus)
            {
                <div class="col-md-6 mb-2">
                    <div class="card text-center d-flex align-items-center" style="width:18rem;margin:20px;">
                        <img class="card-img-top img-fluid mx-auto " src="/Pictures/@item.PictureName" style="max-width:50%" alt="@item.Name">
                        <div class="card-body">
                            <h5 class="card-title">@item.Name</h5>
                            <p class="card-text">@item.Description</p>
                            <div class="form-check">
                                <input type="checkbox" id="ExtraMat_@item.Id" name="SelectedExtraMats" value="@item.Id" />
                                <label class="form-check-label" for="ExtraMat_@item.Id">Select</label>
                            </div>
                            <div class="form-group">
                                <label asp-for="OrderCounts[@item.Id]" class="control-label"></label>
                                <input type="number" id="OrderCount_@item.Id" name="OrderCounts_@item.Id" class="form-control" onchange="updateTotal(@item.Price, '@item.Id')" value="1" min="1" />
                                <span asp-validation-for="OrderCounts[@item.Id]" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label class="control-label">Total</label>
                                <span id="Total_@item.Id">@item.Price</span>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
 *@
 @using MVCGrup2.Areas.Customer.Models
@using MVCGrup2.Entities.Concrete

@model OrderViewModel


<hr />
<div class="row">
    <div class="col-md-6 mb-2">
        <form asp-action="Create" method="post">
            @foreach (ExtraMat item in ViewBag.Extras)
            {
                <div class="card" style="width: 18rem;">
                    <img class="card-img-top" src="/Pictures/@item.PictureName" alt="@item.Name">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <p class="card-text">@item.Description</p>
                        <div class="form-check">
                            <input type="checkbox" id="ExtraMat_@item.Id" name="SelectedExtraMats" value="@item.Id" />
                            <label class="form-check-label" for="ExtraMat_@item.Id">Select</label>
                        </div>
                        <div class="form-group">
                            <label asp-for="OrderCounts[@item.ExtraCount]" class="control-label"></label>
                            <input type="number" id="OrderCount_@item.Id" name="OrderCounts_@item.Id" class="form-control" onchange="updateTotal(@item.Price, '@item.Id')" value="1" min="1" />
                            <span asp-validation-for="OrderCounts[@item.Id]" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label class="control-label">Total</label>
                            <span id="Total_@item.Id">@item.Price</span>
                        </div>
                    </div>
                </div>
            }
            <button type="submit" class="btn btn-primary">Submit Order</button>
        </form>
    </div>
    <div class="col-md-6 mb-2">
        <form asp-action="Create" method="post">
            @foreach (Menu item in ViewBag.Menus)
            {
                <div class="card" style="width: 18rem;">
                    <img class="card-img-top" src="/Pictures/@item.PictureName" alt="@item.Name">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <p class="card-text">@item.Description</p>
                        <div class="form-check">
                            <input type="checkbox" id="ExtraMat_@item.Id" name="SelectedExtraMats" value="@item.Id" />
                            <label class="form-check-label" for="ExtraMat_@item.Id">Select</label>
                        </div>
                        <div class="form-group">
                            <label asp-for="OrderCounts[@item.MenuCount]" class="control-label"></label>
                            <input type="number" id="OrderCount_@item.Id" name="OrderCounts_@item.Id" class="form-control" onchange="updateTotal(@item.Price, '@item.Id')" value="1" min="1" />
                            <span asp-validation-for="OrderCounts[@item.Id]" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label class="control-label">Total</label>
                            <span id="Total_@item.Id">@item.Price</span>
                        </div>
                    </div>
                </div>
            }
            <button type="submit" class="btn btn-primary">Submit Order</button>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
