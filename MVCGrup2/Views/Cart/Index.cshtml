@model IEnumerable<MVCGrup2.Entities.Concrete.Cart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BurgerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExtraMatId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DrinkId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MVCGrup2UserId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BurgerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtraMatId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrinkId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MVCGrup2UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
